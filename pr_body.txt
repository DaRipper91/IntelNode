🎯 **What:** The testing gap in `ShizukuHelper.run` addressed. Modifying `ShizukuHelper` to use a `processRunner` static variable to allow test overriding.
📊 **Coverage:** Covered successful `init` checking `rish`, failures checking `rish`, and `run` command properly calling either `rish` or `sh` based on availability.
✨ **Result:** Full 100% test coverage for the `ShizukuHelper` functionality, ensuring robust handling of Shizuku execution modes.
