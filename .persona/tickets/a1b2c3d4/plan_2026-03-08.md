# Externalize Bootstrap Scripts and Clean Workflow Strings Implementation Plan

## Overview
Refactor `lib/workflow.dart` to improve maintainability and transparency by externalizing shell scripts and cleaning up technical debt (lint warnings).

## Scope Definition (CRITICAL)
### In Scope
- Move `start-arch.sh` and `start-desktop` from `patch.tar.gz` to `assets/scripts/`.
- Modify `Workflow.setupBootstrap` to load and deploy these scripts individually.
- Clean up hardcoded shell strings in `Workflow` methods.
- Fix `duplicate_ignore` lint warnings in `lib/workflow.dart`.
### Out of Scope (DO NOT TOUCH)
- Refactoring `proot` command construction (belongs to a future ticket/phase).
- Implementing atomic rootfs extraction (belongs to a future ticket/phase).

## Current State Analysis
- `lib/workflow.dart:739`: Hardcoded `boot` string.
- `lib/workflow.dart:881-930`: Hardcoded symlink and extraction logic.
- `lib/workflow.dart:966-988`: Hardcoded rootfs post-extraction logic.
- `assets/patch.tar.gz`: Opaque container for bootstrap scripts.

## Implementation Phases

### Phase 1: Asset Reorganization
- **Goal**: Move scripts to a transparent location in the repository.
- **Steps**:
  1. [x] Create `assets/scripts/` directory.
  2. [x] Extract `start-arch.sh` and `start-desktop` from the current `patch.tar.gz`.
  3. [x] Place scripts in `assets/scripts/`.
  4. [x] Update `pubspec.yaml` to include individual script files.
  5. [x] Repackage `patch.tar.gz` without these scripts if other files remain (research showed only `tiny/` folder, likely for mounts).
- **Verification**: `ls assets/scripts/` shows both files. `flutter pub get` succeeds.

### Phase 2: Workflow Refactoring
- **Goal**: Clean up `lib/workflow.dart` and use externalized scripts.
- **Steps**:
  1. [x] Update `Workflow.setupBootstrap` to copy scripts from assets to `G.dataPath/bin/`.
  2. [x] Replace hardcoded symlink logic with a more maintainable loop or config-driven approach.
  3. [x] Fix the 8 `duplicate_ignore` warnings for `use_build_context_synchronously`.
  4. [x] Refactor `Workflow.initForFirstTime` shell blocks into smaller, more readable segments.
- **Verification**: `flutter analyze` returns zero warnings. App successfully completes first-time setup in an emulator or device.

### Phase 3: Verification and Cleanup
- **Goal**: Ensure system integrity.
- **Steps**:
  1. [x] Run `flutter test` to ensure no regressions.
  2. [x] Verify `start-arch.sh` and `start-desktop` are correctly deployed to the container's `/usr/local/bin/`.
- **Verification**: Container boots successfully into XFCE.
